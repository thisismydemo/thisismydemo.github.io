# Repo intent — thisismydemo.github.io

**The static site for the thisismydemo organization — unfilled scaffold README.**

## What this repo is

Published via GitHub Pages for the `thisismydemo` organization (Kris Turner's demo
blog/site). The README itself is the standard HCS repo template with several
`{{placeholder}}` fields never filled in — so beyond "static site, GitHub Pages,
Hugo-based" (per `.hugo_build.lock`, `archetypes/`, `layouts/`, `themes/`), no
project-specific description exists in the repo yet.

## Shape

- Hugo static site (`config.toml`, `content/`, `layouts/`, `themes/`, `static/`)
- `.frontmatter/` — Front Matter CMS config
- Root also carries planning artifacts directly (`blog-improvement-roadmap.md`,
  `fact_check.md`, `social-media-posts.md`) rather than a separate `pmo/`

## Status

Active (live site), **documentation gap** — the README template was never filled
in. Worth fixing next time this repo is touched.
